const FileTree = ({ className = "" }) => {
  return (
    <div
      className={`${className}`}
      style={{ height: "70%", background: "#2B2929", margin: "2rem" }}></div>
  );
};

export default FileTree;
